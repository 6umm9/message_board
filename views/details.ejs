<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        :root {
            --primary: #6366f1;
            --primary-hover: #4f46e5;
            --bg: #f8fafc;
            --card-bg: #ffffff;
            --text-main: #1e293b;
            --text-muted: #64748b;
            --border: #e2e8f0;
        }

        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background-color: var(--bg);
            color: var(--text-main);
            margin: 0;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: var(--primary);
            margin-bottom: 2rem;
        }

        .details-card {
            background: var(--card-bg);
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
            border: 1px solid var(--border);
            width: 100%;
            max-width: 600px;
        }

        .message-user {
            font-weight: 700;
            color: var(--primary);
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .message-text {
            font-size: 1.25rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            white-space: pre-wrap;
        }

        .message-date {
            font-size: 0.875rem;
            color: var(--text-muted);
            margin-bottom: 2rem;
            border-top: 1px solid var(--border);
            padding-top: 1rem;
        }

        .btn-group {
            display: flex;
            gap: 1rem;
        }

        .btn {
            background-color: var(--primary);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: 600;
            transition: background-color 0.2s;
            display: inline-block;
        }

        .btn:hover {
            background-color: var(--primary-hover);
        }

        .btn-secondary {
            background-color: transparent;
            color: var(--text-main);
            border: 1px solid var(--border);
        }

        .btn-secondary:hover {
            background-color: #f1f5f9;
        }
    </style>
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <div class="details-card">
        <div class="message-user">From: <%= message.user %>
        </div>
        <div class="message-text">
            <%= message.text %>
        </div>
        <div class="message-date">Posted on: <%= message.added.toLocaleString() %>
        </div>
        <div class="btn-group">
            <a href="/" class="btn btn-secondary">Back to Board</a>
        </div>
    </div>
</body>

</html>